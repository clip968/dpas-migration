# 2026-06-29

## 세션 시작 시 현재 위치 복구

- 현재 주제는 단일 CPU CP/polled direct I/O hang을 `max_no_lock` budget 없이 5.18식 task-state wakeup 감지로 설명하고 검증하는 것이다.
- 현재 `dpas-kernel/block/blk-mq.c`의 `__blk_hctx_poll()`에는 `stateful_wait`, `task_is_running(current)`, `poll_countp = UINT_MAX`, `TASK_RUNNING` 복구가 들어가 있다.
- `max_no_lock`/`pas_max_no_lock` budget 경로는 제거된 상태로 이해한다.

## 오늘 볼 것

- 현재 코드의 요점을 시각적으로 다시 정리한다.
- 핵심은 CP busy poll을 100회 budget으로 끊는 것이 아니라, sleep 상태에서 들어온 poller가 wakeup되었을 때 상위 wait loop로 돌아가게 하는 것이다.

## sync DIO wait loop 이해

- `set_current_state(TASK_UNINTERRUPTIBLE)`은 곧바로 CPU를 재우는 동작이 아니라, completion handler의 wakeup을 놓치지 않도록 "이 task는 기다리는 중"이라고 상태를 먼저 표시하는 단계다.
- 원래 `v7.1-rc4` import에는 이 wait loop와 `blk_io_schedule()`은 있었지만, sync DIO에서 `bio_poll()`로 기다리는 branch는 없었다.
- 현재 sync DIO polled wait branch는 `347554c8d dio의 hipri path 복구`에서 `dio->submit.poll_bio` 추적과 함께 복구된 것이다.

## 학습 노트 업데이트

- `history/DPAS-학습-노트.md`의 3.3 뒤에 `TASK_UNINTERRUPTIBLE` 표시 후 `bio_poll()`을 호출하는 이유를 추가했다.
- 같은 위치에 원래 `v7.1-rc4` import에는 wait loop/`blk_io_schedule()`만 있었고, sync DIO `bio_poll()` branch는 `347554c8d`에서 복구된 것임을 정리했다.

## 4KB random read 단일 CPU CP 흐름

- `scripts/micro_4krr/run_host_dpas_optane.sh`의 CP 모드는 `io_poll=1`, 기본 `io_poll_delay=-1`, `pas_enabled=0`, `switch_enabled=0`이며 fio는 `--bs=4k --rw=randread --ioengine=pvsync2 --iodepth=1 --hipri`로 실행된다.
- 이 조건에서는 XFS sync direct I/O wait loop가 `TASK_UNINTERRUPTIBLE` 상태를 표시한 뒤 `bio_poll()`을 반복 호출한다.
- `pas_enabled=0`이고 `io_poll_delay=-1`이므로 PAS/LHP sleep은 없고, 실제 진행은 `__blk_hctx_poll()`의 driver poll loop가 맡는다.
- 수정 전 문제는 inner poll loop가 completion/need_resched 전까지 CPU를 오래 붙잡아 단일 CPU에서 상위 `iomap` wait loop가 `dio->submit.waiter`를 다시 확인할 기회를 얻기 어려웠다는 점이다.
- 현재 수정은 budget으로 poll을 끊지 않고, sleep state에서 들어온 poller가 `task_is_running(current)`로 wakeup을 감지하면 `poll_countp = UINT_MAX`로 표시하고 상위 loop로 돌아가게 한다.
- 여기서 wakeup은 SSD가 task를 직접 깨우는 것이 아니라, I/O completion path가 `iomap_dio_done()`에서 `dio->submit.waiter = NULL`로 만들고 `blk_wake_io_task(waiter)`를 호출해 기다리던 task를 runnable 상태로 바꾸는 것을 뜻한다.

## 단일 CPU에서 "안 비키면 여전히 안 비키는 것 아닌가" 정리

- 오늘 헷갈렸던 지점은 "수정 전에는 한 thread가 CPU를 안 비켜서 문제였는데, 수정 후에도 단일 CPU에서 poller가 계속 돌면 결국 똑같은 것 아닌가?"였다.
- 결론: 현재 수정은 다른 thread에게 CPU를 양보시키는 코드가 아니다. `bio_poll()` 안에서 이미 completion/wakeup이 관측됐을 때, inner poll loop가 그 사실을 보고 상위 `iomap` wait loop로 돌아가게 하는 코드다.
- 단일 CPU에서도 I/O completion은 일반 userspace thread가 반드시 한 번 더 스케줄되어야만 생기는 것이 아니다.
  - poll path에서는 현재 fio task 자신이 NVMe completion queue를 직접 확인하고 completion을 처리할 수 있다.
  - interrupt/softirq completion은 일반 thread처럼 scheduler time slice를 받아야만 실행되는 것이 아니라, 현재 CPU에서 실행 중인 task를 끊고 들어올 수 있다.
- 따라서 이 문제를 "다른 thread가 못 돌아서 completion이 불가능했다"로만 보면 반쪽짜리 이해다. 더 정확하게는 "completion/wakeup이 발생해 current task state가 `TASK_RUNNING`으로 바뀌어도, 기존 7.1 poll loop가 그 변화를 탈출 조건으로 보지 못했다"가 핵심이다.
- `need_resched()`는 "스케줄러를 돌릴 이유가 생겼는가"를 보는 조건이지, "내 I/O가 완료되어 wakeup되었는가"를 직접 보는 조건이 아니다. 단일 CPU에서 현재 task가 계속 CPU를 잡고 있고 별도 스케줄링 압력이 없으면, wakeup이 있었어도 `need_resched()`만으로는 상위 wait loop로 빨리 돌아가지 못할 수 있다.
- 그래서 5.18식 핵심은 `get_current_state()`로 stateful wait 진입을 알아두고, poll 중 `task_is_running(current)`가 true가 되면 "completion path가 나를 깨웠다"고 보고 즉시 빠져나가는 것이다.

```text
수정 전 이해:
  poller가 CPU를 안 비킨다
    -> 다른 thread가 못 돈다
    -> completion 확인 기회가 늦거나 사라진다

더 정확한 현재 이해:
  iomap wait loop가 current를 TASK_UNINTERRUPTIBLE로 표시한다
    -> bio_poll() / __blk_hctx_poll() 진입
    -> completion path가 dio->submit.waiter = NULL 처리
    -> blk_wake_io_task(waiter)가 current state를 TASK_RUNNING으로 바꿈
    -> 그런데 기존 poll loop는 이 state 변화를 보지 않음
    -> 상위 iomap wait loop로 못 돌아가 waiter == NULL을 확인하지 못함

현재 수정:
  __blk_hctx_poll()이 task_is_running(current)를 확인
    -> wakeup이 있었음을 감지
    -> poll helper return
    -> iomap wait loop가 waiter == NULL 확인
    -> 완료
```

- 따라서 지금 수정의 의미는 "busy poll을 100번마다 강제로 양보한다"가 아니라, "sleep state에서 들어온 poller가 wakeup되었으면 더 이상 CP 의미로 무한히 돌지 말고 완료 조건을 가진 상위 loop로 복귀한다"이다.

## 학습 노트 추가 업데이트

- `history/DPAS-학습-노트.md`에 `3.3.2 단일 CPU에서 안 비키면 여전히 안 비키는 것 아닌가` 섹션을 추가했다.
- 이 섹션에는 `need_resched()`는 스케줄링 필요 여부이고, `task_is_running(current)`는 completion/wakeup 감지라는 차이를 정리했다.
- 기존 `3.3.2 이 구조가 원래 7.1-rc4에 있었나`는 `3.3.3`으로 밀었다.

## hrtimer 개념 질문

- hrtimer는 kernel 안에서 쓰는 고정밀 알람/타이머로 이해하면 된다.
- 일반 timer가 대략적인 tick 단위 시간 감각이라면, hrtimer는 ns 단위 `ktime_t` 만료 시각을 timerqueue에 넣고 가장 가까운 만료 시각에 맞춰 CPU clock event를 맞춘다.
- 만료 시간이 되면 interrupt/softirq 쪽에서 timer callback을 실행하거나, `hrtimer_sleeper`처럼 잠든 task를 깨운다.
- `dpas-kernel/block/blk-mq.c`의 `blk_mq_poll_sleep_nsec()`에서는 LHP sleep을 위해 `hrtimer_sleeper`를 만들고, 현재 task를 `TASK_UNINTERRUPTIBLE`로 표시한 뒤, 타이머가 만료되면 다시 깨어나도록 한다.
- CP(classic polling)는 `q->poll_nsec < 0`이라 `blk_mq_poll_lhp_sleep()`에서 hrtimer sleep을 타지 않는다. CP에서 중요한 wakeup은 hrtimer가 아니라 I/O completion path의 `blk_wake_io_task(waiter)`이며, 현재 poll loop는 `task_is_running(current)`로 그 wakeup을 감지해 상위 wait loop로 돌아간다.
- `hrtimer_sleeper`의 실제 callback은 `hrtimer_wakeup()`이며, 만료 시 `t->task = NULL`로 표시하고 `wake_up_process(task)`를 호출한다. 그래서 `blk_mq_poll_sleep_nsec()`의 `if (hs.task) io_schedule();`은 타이머가 이미 만료되어 깨어난 상태면 굳이 잠들지 않게 하는 안전장치로 볼 수 있다.

## full DPAS FSM 그림 수정

- `history/DPAS-학습-노트.md`의 6.1 상태 기계 그림이 실제 코드와 다르게 `CP -> INT`, `INT -> PAS`처럼 읽히는 문제가 있었다.
- 실제 코드는 `CP -> PAS`, `INT -> OL`이며, `PAS`/`OL` 전이는 각각 `switch_param5` window가 찬 뒤 평가된다.
- 후속 점검에서 세로 흐름 그림과 `source --조건--> target` 목록 모두 시각화로 보기 어렵다는 문제가 있었다.
- 6.1 전이도는 Mermaid가 아니라 Markdown 코드블록 안의 ASCII art로 다시 바꾸고, 화살표에는 번호만 남긴 뒤 조건은 번호 표로 분리했다.
- 전이도와 전이 요약은 `dpas-kernel/block/blk-mq.c`의 `blk_dpas_maybe_switch_mode()`와 `dpas-kernel/block/blk-core.c`의 `blk_dpas_prepare_bio()` 기준으로 맞췄다.

## blk-mq software queue 질문

- `blk_mq_ctx`가 blk-mq에서 말하는 software queue이며, `request_queue` 안에서 `alloc_percpu(struct blk_mq_ctx)`로 per-CPU 할당된다.
- 따라서 "소프트웨어 큐는 CPU 코어당 하나인가?"에 대한 답은 한 block device/request_queue 기준으로는 대체로 "logical CPU당 ctx 하나"가 맞다.
- 단, 이것은 물리 코어가 아니라 Linux가 보는 CPU 번호 기준이고, 여러 ctx가 `tag_set->map[type].mq_map[cpu]`를 통해 하나의 `blk_mq_hw_ctx`로 N:1 매핑될 수 있다.
- `bio` merge 이해에서 주의할 점: 하나의 `request`는 `rq->bio`(첫 bio)와 `rq->biotail`(마지막 bio)을 들고, 여러 bio는 `bio->bi_next` chain으로 모두 보존된다. 따라서 "bio 정보가 마지막 bio에만 들어간다"는 표현은 틀리고, request는 전체 bio chain과 aggregate sector/length를 함께 들고 있다고 보는 게 맞다.

## 소스코드 설명 방식 스킬화

- `history/2026-06-05-bio-request-poll-flow-report.md` 형식의 핵심은 DPAS 전용 flow chart가 아니라, 실제 소스코드를 따라가며 코드 위치, 짧은 코드 조각, 변수 변화, ASCII 그림, 숫자 예시, 마지막 주의점까지 설명하는 방식이라고 정리했다.
- 개인 Codex 스킬은 `dpas-flow-report`가 아니라 `explain-source-code`로 일반화했다.
- 설치 위치는 `/Users/clip968/.codex/skills/explain-source-code`이며, 기존 `/Users/clip968/.codex/skills/dpas-flow-report`는 활성 스킬 목록에서 제거하고 `/private/tmp/dpas-flow-report-backup-20260629`에 백업해 두었다.
- 기본 `quick_validate.py`는 현재 기본 Python에 `yaml` 모듈이 없어 실행 전 실패하는 환경이지만, 같은 frontmatter/name/description 검사와 `agents/openai.yaml` 파싱은 Ruby YAML 파서로 확인했다.

## io_uring 커널 코드 경로 보고서 작성

- `explain-source-code` 방식으로 `history/2026-06-29-io-uring-kernel-code-path-report.md`를 작성했다.
- 기준은 `dpas-kernel/Makefile`의 `7.1.0-rc4`이며, 일반 `IORING_OP_READ/WRITE`가 `io_uring_enter()`에서 `io_submit_sqes()`, `io_read/io_write()`, `file->f_op->read_iter/write_iter`, raw block 또는 XFS/iomap DIO, `blk_dpas_prepare_bio()`, `bio_poll()`, `blk_mq_poll_bio()`, `nvme_poll()`까지 가는 경로를 정리했다.
- `IORING_OP_URING_CMD`는 일반 read/write와 다른 `file->f_op->uring_cmd()` 경로라 보고서의 주의점에서 분리했다.
