# 2026-06-16

## 세션 시작 시 현재 위치 복구

이 파일은 2026-06-15 기록에서 이어받아 새로 만든 일일 현황판이다.

### 현재 위치

- `dpas-kernel`의 full DPAS mode switching 코드는 전날 기준 전체 커널 빌드까지 통과했다.
- 현재 사용자는 이 커널을 host OS에 올리기 전에 먼저 깨끗하게 빌드되는지 확인하려고 한다.

### 이어받은 확인 사항

- 전날 `/tmp/dpas-build` out-of-tree 전체 빌드는 종료 코드 0으로 통과했다.
- VM 부팅, host 부팅, 실제 block I/O, sysfs runtime mode switching은 전날 전체 빌드 검증 범위에는 포함되지 않았다.
- host 부팅용으로는 기존 `build/dpas-kernel-host` out-of-tree build dir와 `7.1.0-rc4-dpas-host-g4be3fefb1311` 계열 설정 기록이 있다.

### 다음에 볼 것

- `build/dpas-kernel-host` 설정 기준으로 현재 `dpas-kernel`이 빌드되는지 확인한다.
- 빌드 확인 후 `/boot` 설치, GRUB entry 갱신, 재부팅 여부는 별도 사용자 결정이 필요하다.

## host용 full DPAS 커널 빌드 확인

사용자가 full DPAS mode 추가 후 host OS에 올리기 전에 먼저 깨끗하게 빌드되는지 확인하자고 요청했다.

확인 결과:

- `build/dpas-kernel-host` out-of-tree 설정으로 전체 빌드를 실행했다.
  - 명령: `rtk make -C dpas-kernel O=../build/dpas-kernel-host -j8`
  - 결과: 종료 코드 0.
- 같은 명령을 재실행해 no-op 상태에서도 종료 코드 0을 확인했다.
  - 출력 핵심: `Kernel: arch/x86/boot/bzImage is ready  (#2)`
- kernel release는 `7.1.0-rc4-dpas-host-gfed57f0e7ff3`이다.
- 주요 산출물:
  - `build/dpas-kernel-host/arch/x86/boot/bzImage`
  - `build/dpas-kernel-host/vmlinux`
  - `build/dpas-kernel-host/System.map`
  - `build/dpas-kernel-host/modules.order`
- module 산출물도 존재한다.
  - 예: `build/dpas-kernel-host/lib/raid6/raid6_pq.ko`
- `dpas-kernel` 내부 working tree는 깨끗하다.
- root repo에서는 `dpas-kernel` gitlink가 `4be3fef...`에서 `fed57f0...`로 이동한 상태로 보인다.

주의:

- 이번 작업은 빌드 검증까지만 수행했다.
- `/boot` 복사, `modules_install`, initramfs 생성, GRUB 수정, 재부팅, host Optane runtime 테스트는 수행하지 않았다.
- 첫 전체 빌드의 오래 걸린 부분은 module final 단계의 BTF 생성이었다.

## README에 RTK 빌드 메모 추가

- `README.md`에 `Build command note`를 짧게 추가했다.
- 내용: `rtk make ...`는 커널 빌드에 동작하지만, 빌드 진행 상황 디버깅, `find` 옵션, 전체 로그 확인에는 raw command 또는 `rtk proxy <cmd>`가 더 편하다는 점.

## 새 host 커널 one-shot 부팅 실패 원인 확인

사용자가 `7.1.0-rc4-dpas-host-gfed57f0e7ff3` 커널로 1회 부팅을 시도했지만 검은 화면만 보였다고 보고했다.

확인 결과:

- 현재 시스템은 기본 Ubuntu 커널 `7.0.0-22-generic`으로 복귀해 있다.
- GRUB `next_entry=`는 비어 있어 one-shot 예약은 소비됐다.
- `/boot/grub/custom.cfg`의 `linux` 줄 끝에 `initrd /boot/initrd.img-...`가 같은 줄로 붙어 있다.
  - 이 상태에서는 GRUB가 initramfs를 별도 `initrd` 명령으로 로드하지 못할 가능성이 높다.
- 따라서 이번 부팅 실패의 1차 원인은 GRUB custom entry 작성 시 `linux` 줄과 `initrd` 줄이 분리되지 않은 것이다.

다음 작업:

- `custom.cfg`에서 `linux ... panic=30`과 `initrd /boot/initrd.img-...`를 반드시 서로 다른 줄로 다시 작성한다.
- `grub-script-check`, `cat /boot/grub/custom.cfg`, `grub-reboot`, `strings /boot/grub/grubenv | grep next_entry` 순서로 재확인한다.

## 새 host 커널 정상 부팅 확인

사용자가 `/boot/grub/custom.cfg`의 `linux`/`initrd` 줄 분리 문제를 수정한 뒤 다시 one-shot 부팅했고, 정상 부팅 검증을 요청했다.

확인 결과:

- 현재 실행 커널은 `7.1.0-rc4-dpas-host-gfed57f0e7ff3`이다.
- `/proc/cmdline`에는 새 kernel image와 `nvme.poll_queues=20 panic=30`이 들어 있다.
- GRUB `next_entry=`는 비어 있어 one-shot 예약은 소비됐다.
- 실제 host namespace 기준 mount 상태:
  - `/`: `/dev/sdc2` ext4 `rw`
  - `/boot/efi`: `/dev/sdc1` vfat `rw`, `iocharset=iso8859-1`
- `systemctl --failed --no-pager` 결과 실패 unit은 0개다.
- `/sys/module/nvme/parameters/poll_queues` 값은 `20`이다.
- `/sys/block/nvme1n1/queue/` 아래 `pas_enabled`, `pas_adaptive_enabled`, `ehp_enabled`, `switch_enabled`, `switch_param1~7`, `io_poll`, `io_poll_delay`가 보인다.
- 현재 `nvme1n1` knob 값은 `io_poll=1`, `io_poll_delay=-1`, `pas_enabled=0`, `pas_adaptive_enabled=0`, `ehp_enabled=0`, `switch_enabled=0`이다.
- `journalctl -k -b -p warning..alert`에서는 접근 가능한 범위 내 kernel warning 이상 로그가 없었다.

한계:

- `dmesg -T`는 현재 사용자 권한에서 `Operation not permitted`로 직접 확인하지 못했다.

## host DPAS 커널 1회 부팅 guideline 작성

사용자가 앞으로 재사용할 수 있도록 host OS에 DPAS 커널을 설치하고 one-shot 부팅하는 절차를 별도 문서로 정리해달라고 요청했다.

- 새 문서: `history/2026-06-16-host-dpas-kernel-boot-guideline.md`
- 포함 내용:
  - host용 build 산출물과 kernel release 확인.
  - root/boot UUID, PARTUUID 확인.
  - `modules_install`, `/boot` 설치, `depmod`, `mkinitramfs`.
  - GRUB `custom.cfg` 작성과 `linux`/`initrd` 줄 분리 주의.
  - `grub-reboot` one-shot 예약.
  - 재부팅 후 kernel, cmdline, mount, systemd, NVMe poll queue, DPAS sysfs knob 확인.
  - 실패 시 기본 커널 복귀와 자주 발생한 문제.

## host full-DPAS one-touch runner 검토

사용자가 새로 만든 `scripts/micro_4krr/run_host_dpas_optane.sh`가 full DPAS를 제대로 테스트하고, `run_all.sh` 실행 방식을 가져왔는지 검증해달라고 요청했다.

확인 결과:

- 문법 검사는 통과했다: `rtk bash -n scripts/micro_4krr/run_host_dpas_optane.sh`.
- `--help`와 root 권한 가드는 동작한다.
- 현재 커널은 `7.1.0-rc4-dpas-host-gfed57f0e7ff3`이고, `/sys/block/nvme1n1/queue/`에는 `switch_param1~7`이 보인다.
- 커널 코드 기준으로 `switch_param1~7` 기본값과 sysfs 항목은 `dpas-kernel/block/blk-mq.c`, `dpas-kernel/block/blk-sysfs.c`에 존재한다.
- 새 러너는 `DPAS_DEVICE_LIST`, `DPAS_IO_MODE`, `DPAS_JOB_LIST`, `DPAS_RW_FLAGS`를 export하므로 `scripts/micro_4krr/parse.py`와 디렉터리 이름은 맞는다.

남은 리스크:

- 새 러너는 `set -uo pipefail`이라 `run_all.sh`의 `set -eu`와 달리 `mkfs`, `mount`, `fio`, sysfs write 실패를 즉시 중단하지 않을 수 있다.
- `xfs_fio_prerun.sh` 실패가 warning 뒤 계속 진행된다. 이 경우 장치 prefill 없이 본 런이 진행될 수 있다.
- 현재 세션에서는 `/sys/block/nvme1n1`은 보이지만 `/dev/nvme1n1`은 보이지 않는다. 이 상태에서 실행하면 장치 대신 로컬 `test_nvme1n1` 디렉터리에 fio가 돌 위험이 있다.
- `findmnt`를 require하지만 실제로 target device mount 여부를 검사하지 않는다.

다음 작업:

- 코드 변경 전에 사용자 승인 필요.
- 권장 수정 방향은 `set -euo pipefail`, `/dev/$device`와 required knob preflight, `findmnt` 기반 mounted-device 방지, prerun/mount/fio/sysfs write 실패 시 즉시 중단이다.

## micro_4krr parsed_data 파싱 방식 확인

사용자가 `[OUTPUT] parsing fio_data -> parsed_data` 이후 생성된 `scripts/micro_4krr/parsed_data/nvme1n1-RR-repeat_5.txt`가 어떻게 파싱되었는지 질문했다.

확인 결과:

- `scripts/micro_4krr/run_host_dpas_optane.sh`가 기본 sweep을 `DEVICES=("nvme1n1")`, `IO_MODE=("INT" "CP" "LHP" "PAS" "DPAS")`, `JOBS=(20 16 8 4 2 1)`, `RR_RW=("RR")`로 잡고 이를 환경변수로 export한다.
- `scripts/micro_4krr/parse.py`는 `fio_data/nvme1n1/RR/<job>T/<mode>/fio_report_<repeat>.log`를 읽는다.
- `parsed_data/nvme1n1-RR-repeat_5.txt`는 repeat 1~5 각각에 대해 `IOPS` 표와 `CPU` 표를 순서대로 붙인 파일이다.
- IOPS는 fio 로그의 `IOPS=` 값만 뽑는다. `62.3k`는 `62.3`, `582k`는 `582.0`처럼 thousand IOPS 단위로 기록된다.
- CPU는 fio 로그의 `usr=%`와 `sys=%`만 더한다. 예: `usr=8.68%, sys=25.15%`이면 `33.83`.
- 주의: `mean_std.py`는 `parse.py`에서 `ascend=1`로 호출되는데, 현재 job 순서는 `20,16,8,4,2,1` 내림차순이다. 따라서 `result_data/nvme1n1-RR-repeat_5.txt`의 평균/표준편차는 행이 섞였을 가능성이 있어 별도 확인 필요.

## CPU 한 개로 단일 코어 contention 테스트 방법

사용자가 CPU를 한 개만 써서 thread 1~8 실험을 하려 했고, 그 방법을 기록해달라고 요청했다.

배경:

- 기존 실험은 fio `--cpus_allowed_policy=split`으로 thread마다 전용 코어를 줘서, CP(classic polling)의 100% busy-poll 비용이 사실상 공짜였다. 즉 IOPS만 보면 CP에 유리하게 편향된 실험이다.
- 여러 thread를 한 코어에 몰아 CPU 경합을 만들면 CP의 busy-poll은 thrashing으로 불리해지고, PAS/DPAS의 sleep-yield가 유리해진다. DPAS의 가치는 이 경합 환경에서 드러난다.

방법 (코드 수정 불필요):

- `scripts/micro_4krr/run_host_dpas_optane.sh`는 `/proc/self/status`의 `Cpus_allowed_list`를 읽어 fio `--cpus_allowed`를 정한다. 따라서 프로세스를 `taskset`으로 한 코어에 묶으면 스크립트가 자동으로 그 코어만 쓴다.
- 검증: `taskset -c 0 awk ... /proc/self/status` → `0`, `taskset -c 2` → `2`, 묶지 않으면 `0-19`.
- 실행 예:

```bash
cd /home/urop1/dpas-migration/scripts/micro_4krr
sudo taskset -c 0 env DPAS_JOB_LIST=1,2,4,8 DPAS_RUNTIME=10 ./run_host_dpas_optane.sh --clean
```

- thread 1~8 전부 보려면 `DPAS_JOB_LIST=1,2,3,4,5,6,7,8`. 다른 코어로는 `taskset -c 2`. 안정성 위해 `DPAS_REPEATS=3` 권장.
- `taskset`은 스크립트 전체(자식 fio 포함)를 한 코어에 묶으므로 "1코어만 가용" 상황을 충실히 재현한다. fio 인자만 바꾸는 방식보다 정확하다.

볼 포인트:

- IOPS: 2~8T에서 CP의 우위가 사라지거나 역전되어야 정상이다(1코어에서 N개 thread busy-poll → thrashing). PAS/DPAS가 CP를 IOPS로 이겨야 한다.
- DPAS가 CP를 피하고 PAS/INT로 가는지. 경합 환경에서는 polling을 줄이는 게 옳은 선택이다.

스크립트 변경 이력:

- 단일 CPU 편의용으로 `run_host_dpas_optane.sh`에 `DPAS_CPU_ALLOWED` env override를 잠깐 추가했다가, `taskset`으로 충분하고 더 정확해서 다시 원복했다. 현재 스크립트에는 해당 옵션이 없다(기능 변화 없음, `bash -n` 확인 완료).

## 로컬 미추적 기록 병합

아래 내용은 pull 전 로컬에 미추적 상태로 있던 `history/2026-06-16.md`에서 보존한 기록이다. 원격의 같은 날짜 기록과 일부 맥락이 겹치지만, 당시 학습 노트 수정 흐름을 잃지 않기 위해 별도 섹션으로 이어 붙였다.

### 로컬 세션 시작 시 현재 위치 복구

이 파일은 2026-06-15 기록에서 이어받아 새로 만든 일일 현황판이다.

#### 현재 위치

- `dpas-kernel`의 full DPAS mode switching 코드는 전체 커널 빌드까지 통과한 상태로 기록되어 있다.
- 아직 VM 부팅, 실제 block I/O, sysfs runtime mode switching 동작은 별도 검증이 필요하다.
- 사용자는 `history/DPAS-학습-노트.md`를 읽는 중이며, 변수 변화와 실제 수정 이유가 충분히 이해되지 않는다고 지적했다.

#### 오늘 한 작업

- `history/DPAS-학습-노트.md` 앞부분에 원래 Linux block polling path와 DPAS가 비트는 지점을 분리해서 설명하는 학습 지도를 추가했다.
- `kiocb->ki_flags`, `bio->bi_opf`, `bio->bi_cookie`, `dpas_mode`, `dpas_*_cnt`, `dpas_qd_sum`, `dpas_tf`, PAS `stat` 필드가 어느 함수에서 왜 바뀌는지 표와 시나리오로 보강했다.
- PAS mode와 INT mode의 변수 타임라인, `sr_last/update_req/dur` 갱신이 한 박자 늦게 일어나는 이유, full DPAS 전환 변수 해석표를 추가했다.
- 외부 배경 자료로 Linux kernel 공식 blk-mq 문서와 queue sysfs 문서를 노트에 연결했다.

#### 다음에 볼 것

- 학습 노트의 새 0절, 3.4절, 3.5절, 4.4.1절, 6.1.1절을 먼저 읽고 변수 흐름이 이어지는지 확인한다.
- 여전히 헷갈리는 지점은 `REQ_POLLED`가 "요청 표시"인지 "실제 완료 처리"인지, `bi_cookie`가 request tag인지 hctx selector인지부터 다시 점검한다.
- 구현 검증으로 돌아가면 runtime trace에서 `blk_dpas_prepare_bio() -> bio_poll() -> blk_mq_poll_bio() -> nvme_poll()` 호출과 mode 전환을 실제 I/O로 확인해야 한다.

### DPAS 학습 노트 표 중심 구조 제거

사용자가 Notion 업로드를 중단하고, `history/DPAS-학습-노트.md`의 표 중심 구성을 폐기한 뒤 시각화와 문장 단위 설명으로 교체해달라고 요청했다.

변경:

- 코드블럭 밖 Markdown 표를 모두 제거했다.
- 변수 변화 지도, 수정 이유, 모드 요약, PAS 숫자 예시, PAS/LHP 비교, full DPAS 전환 변수, 5.18 vs 7.1 비교, 코드 위치 목록을 흐름도·ASCII 상태 변화·짧은 문단 설명으로 바꿨다.
- 코드블럭 안에 남아 있던 `cur_case` 표 형태 설명도 분기형 문장 설명으로 바꿨다.

검증:

- 코드블럭 밖 Markdown 표가 0개인지 확인했다.
- 코드펜스 개수가 짝수인지 확인했다.
